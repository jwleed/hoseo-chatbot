spring.application.name=hoseo-chatbot
server.port=8080
server.http2.enabled=false

rag.server.url=https://wriggle-script-caption.ngrok-free.dev

spring.jackson.property-naming-strategy=SNAKE_CASE

# 실행 시 -Dspring.profiles.active=local 또는 dev 로 선택
# local : H2 인메모리 (기본 개발용)
# dev   : MySQL Docker (팀 협업용)